<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Poll Results - {{ $poll->poll_title }}</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
            color: #333;
        }
        .header {
            text-align: center;
            margin-bottom: 30px;
            border-bottom: 2px solid #007bff;
            padding-bottom: 20px;
        }
        .poll-title {
            font-size: 24px;
            font-weight: bold;
            color: #007bff;
            margin-bottom: 10px;
        }
        .poll-info {
            font-size: 14px;
            color: #666;
        }
        .question {
            margin-bottom: 30px;
            page-break-inside: avoid;
        }
        .question-title {
            font-size: 18px;
            font-weight: bold;
            margin-bottom: 15px;
            color: #333;
        }
        .option {
            margin-bottom: 10px;
            padding: 8px;
            border-left: 4px solid #007bff;
            background-color: #f8f9fa;
        }
        .option-text {
            font-weight: bold;
            margin-bottom: 5px;
        }
        .option-stats {
            font-size: 14px;
            color: #666;
        }
        .progress-bar {
            width: 100%;
            height: 20px;
            background-color: #e9ecef;
            border-radius: 10px;
            margin-top: 5px;
            overflow: hidden;
        }
        .progress-fill {
            height: 100%;
            background-color: #007bff;
            border-radius: 10px;
        }
        .summary {
            margin-top: 40px;
            padding: 20px;
            background-color: #e3f2fd;
            border-radius: 8px;
        }
        .summary-title {
            font-size: 16px;
            font-weight: bold;
            margin-bottom: 10px;
        }
        .footer {
            margin-top: 40px;
            text-align: center;
            font-size: 12px;
            color: #999;
            border-top: 1px solid #ddd;
            padding-top: 20px;
        }
    </style>
</head>
<body>
    <div class="header">
        <div class="poll-title">{{ $poll->poll_title }}</div>
        <div class="poll-info">
            Generated on {{ now()->format('M d, Y h:i A') }} | 
            Status: {{ ucfirst($poll->status) }} | 
            Total Votes: {{ $poll->votes->count() }}
        </div>
        @if($poll->poll_description)
            <div style="margin-top: 15px; font-style: italic;">{{ $poll->poll_description }}</div>
        @endif
    </div>

    @foreach($poll->questions as $question)
        <div class="question">
            <div class="question-title">{{ $question->question_text }}</div>
            
            @php
                $totalVotes = $question->votes->count();
            @endphp
            
            @foreach($question->options as $option)
                @php
                    $optionVotes = $option->votes->count();
                    $percentage = $totalVotes > 0 ? round(($optionVotes / $totalVotes) * 100, 1) : 0;
                @endphp
                
                <div class="option">
                    <div class="option-text">{{ $option->option_text }}</div>
                    <div class="option-stats">{{ $optionVotes }} votes ({{ $percentage }}%)</div>
                    <div class="progress-bar">
                        <div class="progress-fill" style="width: {{ $percentage }}%"></div>
                    </div>
                </div>
            @endforeach
            
            @if($totalVotes === 0)
                <div style="text-align: center; color: #999; padding: 20px;">
                    No votes recorded for this question
                </div>
            @endif
        </div>
    @endforeach

    <div class="summary">
        <div class="summary-title">Poll Summary</div>
        <div><strong>Total Participants:</strong> {{ $poll->votes->groupBy('user_id')->count() }}</div>
        <div><strong>Total Votes:</strong> {{ $poll->votes->count() }}</div>
        <div><strong>Poll Created:</strong> {{ $poll->formatted_creation_date }}</div>
        <div><strong>Poll Status:</strong> {{ ucfirst($poll->status) }}</div>
        @if($poll->end_date)
            <div><strong>End Date:</strong> {{ $poll->formatted_end_date }}</div>
        @endif
    </div>

    <div class="footer">
        This report was generated by the Polling & Voting System
    </div>
</body>
</html> 